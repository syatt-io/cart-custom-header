import { comparatorCurry } from './types';
export default function reducer<T>(state: Array<T>, action: Action<T>): import("./types").Tree<T>[];
export declare enum ActionType {
    Check = 0,
    Uncheck = 1,
    Toggle = 2,
    BulkCheck = 3,
    BulkUncheck = 4
}
export declare type Action<T> = {
    type: ActionType;
    item?: T;
    checked?: Array<T>;
    itemToToggle?: {
        item: T;
        nodesKey?: string;
        comparator?: comparatorCurry<T>;
    };
};
